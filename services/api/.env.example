# IP 기준 요청 제한. 초기 제안값(분당 30회)이며, 실측 후 조정한다.
RATE_LIMIT_MAX_REQUESTS=30
RATE_LIMIT_WINDOW_SECONDS=60

# 신뢰할 수 있는 리버스 프록시(nginx 등) 뒤에 배포했을 때만 true로 켠다.
# 켜면 X-Forwarded-For의 첫 값을 클라이언트 IP로 신뢰한다 - 프록시 없이 켜두면
# 누구나 이 헤더를 직접 보내 요청 제한을 우회하거나 남을 사칭할 수 있다.
TRUST_PROXY_HEADERS=false
