# TrueTitle 작업 규칙

## 시작 전

- 사용자 최신 요청과 실제 파일 상태를 먼저 확인합니다. 질문·점검 요청은 수정 허가가 아닙니다.
- Git 저장소와 기본 폴더, 샘플 확인 코드가 있으며 정식 데이터 다운로드까지 진행했습니다. 실제 파일을 확인하고 모델·API·확장프로그램·배포가 완료되었다고 가정하지 않습니다.
- 읽기와 분석은 가능하지만 파일 수정·설치·실행 범위는 사용자 승인을 따릅니다.
- 수정 전 범위·이유·영향·백업·검증 계획을 설명합니다. 기존 파일은 .codex-backups/YYYYMMDD-HHMMSS/에 원래 상대 경로로 백업하고 해시를 확인합니다. 실패하면 수정하지 않습니다.
- 작업 후 docs/CODEX_CHANGELOG.md에 요청·승인, 백업, 파일별 변경, 이유·영향, 실제 검증·미검증, 복구 방법을 기록합니다.
- 기존 사용자 변경을 덮어쓰지 않습니다. 새 파일은 덮어쓸 기존 파일이 없는지 확인하고, 신규 생성으로 기록합니다.

## 프로젝트 범위

- 로그인 없는 PC Chrome 확장프로그램. 한국어 기사 페이지에서 제목과 본문을 읽고 낚시성·과장·제목과 본문의 불일치 신호를 분석합니다. 초기 대상은 네이버 뉴스 일반 기사이며 사이트 지원 여부를 실제로 검증합니다.
- 최종 사용 흐름은 기사 진입 후 분석입니다. 개발 첫 단계는 버튼으로 추출·분석하며, 자동 분석도 사용자 동의와 ON 상태에서만 수행합니다.
- 확장프로그램은 WXT + React + TypeScript / Manifest V3, 분석 서버는 Python + FastAPI입니다. scikit-learn 베이스라인과 한국어 사전학습 모델 파인튜닝을 비교하는 계획이며 라이브러리 호환성·실행 환경은 설치 전에 확인합니다.
- AI Hub 정식 Part1 라벨링 데이터의 Training·Validation을 사용합니다. newTitle과 newsContent가 입력이고 clickbaitClass는 0=낚시성, 1=비낚시성입니다. 원천데이터·Part2·Sample을 임의로 합치지 않습니다.
- 기사 ID·본문 중복과 학습/검증 간 누출을 확인합니다. 원래 제목·가공 방식 등의 메타데이터를 모델 입력에 섞지 않습니다. 분류 정답이 개별 불일치 유형이나 사실 여부를 보장한다고 해석하지 않습니다.
- 핵심 요약·설명은 분류 모델 출력과 별도 기능입니다. 본문에 근거해야 하며 외부 AI 호출·비용·데이터 전송은 별도 확인합니다.
- 낚시성 신호를 표시할 뿐, 가짜뉴스 확정이나 진위 확률을 표시하지 않습니다.
- 로그인·DB·NestJS·Next.js·새 데이터셋·범위 밖 모델을 임의로 추가하지 않습니다.
- 데이터 출처는 AI Hub 낚시성 기사 탐지 데이터입니다. NIA 사업 결과임을 표기하고 재배포·국외 반출 제한을 준수합니다. 현재 다운로드 폴더는 Git 제외 대상이며 외부 클라우드 동기화 여부는 별도 확인합니다.

## 담당 경계

- A(진현): services/api/training/, app/ml/, app/api/predict·explain, app/schemas/, 관련 테스트. 데이터 전처리·학습·평가·추론·설명 API를 담당합니다.
- A 관리: API 계약과 shared/의 API 타입. 설정 타입 등 공동 경계 변경은 B와 합의.
- B(박소현): 확장 entrypoints/, sites/, detection/, explanation/, settings/, 기사 제목·본문 추출, 팝업·결과 화면, 통신·브라우저 테스트, WXT·패키지 설정.
- B: 서버 app/main.py, app/core/, health·ready, 공통 의존성, Docker, CI, 운영 문서.
- 공통 진입 파일을 양쪽 AI가 동시에 덮어쓰지 않습니다. 담당 변경은 두 사람이 합의합니다. 카카오톡으로 소통하며 별도 Issue·계약 문서 작성을 강제하거나 사용자가 삭제한 문서를 재생성하지 않습니다.
- B가 Background의 통신 모듈에서 분석 API를 호출하고 결과·로딩·오류를 UI에 전달합니다. URL/title/body는 요청 후보이며 실제 전송 범위·엔드포인트·응답 타입·오류 처리는 연결 전에 두 사람이 확정합니다.

## 안전·데이터

- 동의 전·OFF·초기화 후에는 제목·본문·URL을 전송하지 않습니다. 최소 전송 차단을 실제 모델 연결 전에 구현합니다.
- 초기화 후 OFF 및 재동의 상태로 복귀하고 큐·캐시·표시를 정리합니다.
- 동의 후 기사 제목·본문만 필요한 범위에서 추출해 고정 API로 전송합니다. URL은 용도·필요성을 합의한 경우에만 포함하며 불필요한 쿼리·fragment는 제외합니다. 쿠키·검색어·방문 기록·광고·댓글은 수집하거나 전송하지 않습니다.
- 제목과 요청 본문을 서버 로그에 남기지 않습니다. 원본 데이터·모델·비밀값·개인 브라우저 프로필은 Git에 올리지 않습니다.
- 오류를 0점·정상 분석으로 바꾸지 않습니다. 개발용 mock은 명확히 구분하고 운영 결과처럼 시연하지 않습니다.

## Git과 검증

- Git 초기화·원격 연결·commit·push·merge·switch·reset·restore·clean·rebase 등 상태 변경은 별도 명시적 승인 없이 하지 않습니다.
- Git 저장소는 연결되어 있으며 현재 브랜치·원격 상태는 작업 시 읽기로 확인합니다. Git 변경은 사용자가 직접 하거나 별도로 승인한 범위에서만 진행합니다.
- 가능한 읽기 검사: git status, git diff. 아직 저장소가 없으면 그 사실을 보고합니다.
- 의존성 설치·비용 발생·외부 배포·원격 저장소 생성도 명시적 승인이 필요합니다.
- 현재 실행 가능한 npm scripts·pytest 설정은 없습니다. 빌드·테스트 명령을 완료했다고 꾸미지 않습니다.
- 초기 개발 시 패키지 버전과 실제 설치·실행·검증 명령을 확인하고 README 또는 docs/setup.md에 기록합니다.
- 두 AI는 대화 기억을 공유하지 않습니다. 최신 코드·API 계약·결정 기록·실제 진행 상황으로 동기화합니다.
